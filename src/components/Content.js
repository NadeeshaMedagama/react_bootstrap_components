import React from 'react';
import './paragraph.css';

export default function Content()  {
    return (
        <div className="paragraph">
            <p> In React, component names should be capitalized when you use them as JSX elements. This is a common convention to distinguish components from regular HTML elements.</p>

        </div>
    );
};


